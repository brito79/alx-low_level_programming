#include "main.h"
/**
 * _abs - Compute the absolute value
 * @i: The number to be checked
 *
 * Return: The positive n Always
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}

